#### Get mysql container up

```bash
docker-compose up -d
```

#### Connect mysql container

```bash
docker exec -it mysql mysql -u root -p
```

#### API Docs: Swagger

http://localhost:8080/swagger-ui.html
